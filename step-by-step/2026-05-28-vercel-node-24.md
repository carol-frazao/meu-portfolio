# Correção: Node.js 16.x descontinuado na Vercel

## Arquivos alterados

| Arquivo | Função | Justificativa | Impacto |
|---------|--------|---------------|---------|
| `package.json` | Campo `engines.node: "24.x"` | Vercel lê a versão do Node no repositório | Deploy usa Node 24 em vez de 16 |
| `.nvmrc` | Versão `24` para ferramentas locais/nvm | Alinha ambiente local com produção | Consistência dev/prod |
| `vercel.json` | `installCommand` e `buildCommand` vazios | Site estático (HTML/CSS/JS já compilados) | Evita `npm install` com dependências antigas no deploy |

## Ação manual na Vercel

Em **Project Settings → General → Node.js Version**, altere de `16.x` para `24.x` e faça um novo deploy.
